[tool.poetry]
name = "gringo-crawler"
version = "0.1.0"
description = "stub"
authors = ["Noam"]

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
